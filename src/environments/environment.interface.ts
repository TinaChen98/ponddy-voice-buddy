export interface EnvironmentInterface {
  production: boolean;
  apiURL: string;
  devApiURL: string;
  authApiUrl: string;
  devAuthApiUrl: string;
  clientId: string;
  devClientId: string;
  apiUrl: string;
  devApiUrl: string;
  redirectUri: string;
  devRedirectUri: string;
}
